module.exports = class DepthCalculator {

    constructor(){
        this.depth = 1;
        this.maxDepth = 1;
    }

    increaseDepth(){
        this.depth++;
    }

    calculateDepth(arr) {
        arr.forEach(el => {

        });
    }
};